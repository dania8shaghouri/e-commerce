const ApplyFilterButton = () => {
  return (
    <button className="mt-2 w-full rounded-xl bg-primary py-3 font-semibold text-white transition hover:bg-primaryHover">
      Apply Filters
    </button>
  );
};

export default ApplyFilterButton;
